require "spec_helper"

RSpec.describe RspecExternalTestsTrigger do
  it "has a version number" do
    expect(RspecExternalTestsTrigger::VERSION).not_to be nil
  end

  it "does something useful" do
    #expect(false).to eq(true)
  end
end
